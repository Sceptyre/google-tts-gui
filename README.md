# Google TTS as a gui to generate audio files
requires the .json file pulled from the cloud console dropped into the root of project and renamed to .apiKey

# Init
`pip install -r requirements.txt`

# Run
`py app.py`
